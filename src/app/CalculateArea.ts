export class CalcualreArea {

  public getArea(width: number, length: number): number {
    return width * length;
  }

  public getPerimeter(width: number, length: number) {
    return 2 * (width + length);
  }
}
